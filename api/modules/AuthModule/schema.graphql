type Query {
  login(uid: String!, password: String!): AuthToken
  me: User
}

type AuthToken {
  token: String!
}

type User {
  id: ID!
  email: String!
  firstName: String!
  lastName: String!
  username: String!
}